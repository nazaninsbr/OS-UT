#include <stdio.h>


int main(){
	int x =6;
	int y = 23;
	float div = y/x;
	printf("the sum is: %d, the subtraction is:%d,the multiplication is:%d, the modules is:%d, and the division is:%f", x+y, x-y, x*y, y%x, div);
	return 0;
}